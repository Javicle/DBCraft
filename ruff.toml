
exclude = [
    ".venv",
    "venv",
    ".git",
    "__pycache__",
]
[lint]
select = ["E", "W", "F", "I"] # Ошибки, предупреждения, импорты
ignore = ["E501"] # Если хочешь игнорировать длину строки (форматтер сам правит)
